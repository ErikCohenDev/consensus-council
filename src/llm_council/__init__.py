"""Compatibility package to support imports like `llm_council.*`.

This package re-exports modules from the flat `src/` layout to maintain
backward-compatibility with tests and docs that import `llm_council.*`.
"""

